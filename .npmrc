# Optional: Configure npm settings
# Uncomment and configure as needed

# Use a specific registry
# registry=https://registry.npmjs.org/

# Save exact versions
save-exact=true

# Engine strict (enforce Node version)
engine-strict=true

